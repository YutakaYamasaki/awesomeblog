<li>
	<%= gravatar_for(user, 50) %>
	<%= link_to user.name, "/users/#{user.id}"  %>
	<% if current_user.admin? && !(current_user == user) %>
	|<%#= link_to "編集", edit_path(user),method: :post %>
	|<%= link_to "Delete", user_path(user), method: :delete,
													data:{confirm:"You sure?" } %>

	<% end %>
</li>